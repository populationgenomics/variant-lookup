# Python
__pycache__/
*.py[cod]
*$py.class
*.so
build/
dist/
*.egg-info/
*.egg

# Virtual environments
.venv/
venv/

# Test / lint caches
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editor / OS
.DS_Store
.idea/
.vscode/

# Deployment / secrets — never commit these.
.env
.env.local
api-keys.yaml

# Vendored AGPL sources used at deploy time (see scripts/setup_variantvalidator.sh).
# Keep separate from gateway source so the boundary stays obvious.
vendor/
